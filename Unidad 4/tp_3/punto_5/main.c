// Ingresar 30 valores.Contar cuantos están entre 1 y 10,
// cuantos entre 10 y 20, cuantos entre 20 y 30 y cuantos son mas de 30.
// Luego mostrar el porcentaje de cada grupo en el total.

#include <stdio.h>
#include "../scan_int.h"
#define MAX 30

int main (){
    int oneToTen, tenToTwenty, twentyToThirty, x;

    for (int i = 0; i < MAX; i++)
    {
        
    }
    

    return 0;
}